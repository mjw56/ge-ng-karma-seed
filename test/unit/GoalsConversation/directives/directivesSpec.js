//
// test/unit/GoalsConversation/directives/directivesSpec.js
//
describe("Unit: Testing Goals Conversation Directives", function() {

  var $compile, $rootScope;

  beforeEach(angular.mock.module('goalExplorer'));

  beforeEach(inject(
    ['$compile','$rootScope', function($c, $r) {
      $compile = $c;
      $rootScope = $r;
    }]
  ));

  // TODO: insert necessary specs
});
